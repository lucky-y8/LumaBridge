# 已知问题与验收边界

- 当前已完成自动化测试和无界面平台下的 Qt 启动冒烟测试；仍需在真实 Windows 11 x64、1366×768 屏幕和干净虚拟机中执行需求文档的人工验收。
- Linux 包必须在拟支持的 Linux 发行版上单独构建和测试；Windows 构建不能复用。
- OpenCV 能否打开特定 MOV/MKV 文件取决于打包时附带的 FFmpeg 编解码能力。无法打开的单个录像会被标记并跳过。
- 手机拍摄质量决定分片收集率。校验只能保证“成功结果逐字节正确”，不能保证任意录像都能收齐。
- v1.0 按需求不包含加密；`.qrstate` 中含已恢复的原始分片，应按原文件相同的敏感级别保管。
- 尚未提供项目图标和代码签名证书；Windows 初次运行时可能显示未知发布者提示。

