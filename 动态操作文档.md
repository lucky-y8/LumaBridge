# 动态二维码离线文件传输操作文档

## 1. 适用场景

电脑无法联网、USB 无法使用，但电脑可以显示二维码，手机可以录像。文件通过以下路径传输：

```text
离线电脑 → 动态二维码 → 手机录像 → 接收电脑 → 恢复原文件
```

本方案不会把文件上传到网络。每个二维码都包含分片编号和 CRC32 校验；恢复后还会校验整个文件的 SHA-256。

## 2. 所需文件和环境

离线电脑（发送端）：

- Windows、Python 3.11；
- `qrcode`；
- `Pillow`；
- 本文附带的 `qr_sender.py`。

接收电脑：

- Python 3.11 或兼容版本；
- `opencv-python`；
- `zxing-cpp`；
- 本文附带的 `qr_video_restore.py`。

离线安装包由使用者自行准备。发送端若已经可以执行 `from PIL import Image`，只需补充 `qrcode-*-py3-none-any.whl`。

### 2.1 判断 Python 位数

```powershell
python -c "import platform;print(platform.architecture()[0])"
```

输出 `64bit` 时，Pillow 应选择名称包含 `cp311-cp311-win_amd64` 的 wheel；输出 `32bit` 时选择 `cp311-cp311-win32`。

### 2.2 安装和验证发送端依赖

在 wheel 所在目录执行（实际文件名以下载结果为准）：

```powershell
python -m pip install --no-index qrcode-*-py3-none-any.whl
```

如果 Pillow 尚未安装：

```powershell
python -m pip install --no-index pillow-*-cp311-cp311-win_amd64.whl
```

验证：

```powershell
python -c "import qrcode;from PIL import Image;print('OK')"
```

## 3. 离线电脑生成并播放动态二维码

将以下文件放在同一目录：

```text
data.7z
qr_sender.py
```

打开 PowerShell，进入该目录后运行：

```powershell
python qr_sender.py data.7z
```

默认参数为每个二维码保存 700 字节、每秒播放 3 张。600KB 文件约有 880 个分片，一轮大约 5 分钟。

如果二维码识别困难，可以降低密度和速度：

```powershell
python qr_sender.py data.7z --chunk-size 500 --fps 2
```

控制键：

| 按键 | 作用 |
| --- | --- |
| `Esc` | 退出 |
| 空格 | 暂停或继续 |
| 左右方向键 | 暂停后切换分片 |

程序会显示文件大小、分片总数和 SHA-256。建议拍摄前保存或拍下 SHA-256。

## 4. 手机录像

1. 手机横屏，选择 1080p/60fps；空间不足时可用 1080p/30fps。
2. 清洁镜头，关闭美颜、滤镜和视频防抖裁切。
3. 手机固定在支架上，镜头尽量正对屏幕。
4. 二维码应完整进入画面，四周保留白边；不要让状态文字遮挡二维码。
5. 点按二维码区域完成对焦，并适当降低曝光，避免屏幕白色区域过曝。
6. 从某一轮的第一个分片开始录像，连续录制至少两个完整循环。
7. 录制结束后检查视频：二维码边缘应清晰，没有严重频闪、反光或摩尔纹。

两个完整循环可显著减少某些分片因切换瞬间、失焦或反光而漏读的概率。

## 5. 接收电脑恢复文件

接收电脑安装依赖：

```powershell
python -m pip install opencv-python zxing-cpp
```

将以下文件放在同一目录：

```text
qr_video.mp4
qr_video_restore.py
```

运行：

```powershell
python qr_video_restore.py qr_video.mp4
```

成功后默认生成：

```text
restored_data.7z
```

也可以指定输出文件：

```powershell
python qr_video_restore.py qr_video.mp4 -o data_recovered.7z
```

程序不会覆盖已有文件。如果提示输出文件已存在，请删除旧结果或换一个输出文件名。

### 5.1 视频清晰但识别率偏低

提高每秒尝试识别的录像帧数：

```powershell
python qr_video_restore.py qr_video.mp4 --attempts-per-second 20
```

识别速度会变慢，但可能找回更多分片。

### 5.2 有多段录像

当前恢复脚本一次读取一个视频。最简单的处理方式是用手机或视频工具无损/高质量合并视频，再运行恢复程序。不要改变画面方向或裁掉二维码白边。

如果第一段视频已经覆盖两个完整循环，一般不需要合并。

## 6. 最终校验

恢复程序自身会核对发送端写入二维码的 SHA-256。看到“恢复成功”表示所有分片齐全，且最终文件哈希一致。

Windows 还可以再次查看哈希：

```powershell
Get-FileHash .\restored_data.7z -Algorithm SHA256
```

检查 7z 文件结构：

```powershell
7z t .\restored_data.7z
```

出现 `Everything is Ok` 才表示压缩包内部结构正常。

## 7. 去重机制与准确性说明

### 7.1 是否会重复写入数据

不会。每个二维码包含文件标识、分片序号、总分片数、CRC32 和分片数据。恢复程序以分片序号为键保存数据：

```python
previous = chunks.get(item["index"])
if previous is not None and previous != item["chunk"]:
    raise RuntimeError(f"分片 {item['index']} 出现冲突")
chunks[item["index"]] = item["chunk"]
```

处理规则如下：

- 同一分片重复出现且内容一致：自动去重，只保留一份；
- 同一序号出现不同内容：立即报错，不继续生成文件；
- 录像包含两个或三个完整循环：不会使恢复文件变大；
- 视频中混入其他文件的二维码：通过文件标识过滤，不会写入当前文件。

因此可以放心录制多个循环，重复二维码不会影响最终结果。

### 7.2 准确性如何保证

准确性分为“是否能收齐”和“收齐后是否正确”两部分：

- **分片识别层**：二维码自身带有纠错能力；无法可靠识别的画面通常会被跳过；
- **分片校验层**：每个分片使用 CRC32 校验，识别错误的分片不会被接受；
- **文件校验层**：所有分片拼接完成后，使用 SHA-256 核对整个文件；
- **压缩包校验层**：最后通过 `7z t` 检查 7z 内部结构。

只有分片全部齐全、文件大小正确并且 SHA-256 完全一致，程序才会显示：

```text
恢复成功
```

如果拍摄质量较差，程序会提示“缺少分片”或校验失败，而不会把不完整文件误报为成功。因此，只要程序显示恢复成功，就表示恢复结果与原文件逐字节一致。

### 7.3 默认参数下的识别机会

默认二维码速度为 3fps。手机使用 60fps 录像时，每张二维码大约会保留 20 个视频帧。恢复程序默认每秒尝试识别 12 帧，相当于每个二维码每轮约有 4 次识别机会；录制两个循环约有 8 次机会。

实际成功率仍受屏幕、摄像头和拍摄环境影响，不能仅根据参数保证百分比。主要影响因素包括：

- 手持晃动或持续失焦；
- 屏幕反光、过曝、摩尔纹或滚动黑条；
- 二维码没有完整进入画面；
- 视频经过聊天软件二次压缩；
- 二维码密度过高或播放速度过快。

推荐正式传输时使用：

```powershell
python qr_sender.py data.7z --chunk-size 700 --fps 3
```

录制两个完整循环。如果出现漏帧，可录制三个循环，或者改用更稳定的参数：

```powershell
python qr_sender.py data.7z --chunk-size 500 --fps 2
```

## 8. 预计录像时长

以 600KB 文件为例：

| 参数 | 单循环时长 | 推荐两循环时长 |
| --- | ---: | ---: |
| `--chunk-size 700 --fps 3`（默认） | 约4分53秒 | 约9分46秒 |
| `--chunk-size 500 --fps 2`（稳定） | 约10分15秒 | 约20分30秒 |
| `--chunk-size 800 --fps 4`（快速） | 约3分12秒 | 约6分24秒 |

使用默认参数时，建议手机录像约 10～11 分钟，为开始和结束操作预留少量时间。手机选择 30fps 或 60fps 只改变视频帧数和识别机会，不改变二维码播放总时长。

## 9. 常见问题

### 完全识别不到二维码

- 确认视频拍摄的是 `qr_sender.py` 生成的二维码；
- 二维码必须完整入镜，并保留四周白边；
- 避免斜拍、反光、过曝和数码变焦；
- 尝试 `--chunk-size 500 --fps 2` 后重新录制；
- 接收端尝试 `--attempts-per-second 20`。

### 提示缺少部分分片

说明已经识别成功，但录像没有覆盖完整循环，或部分二维码持续失焦。重新录制至少两个完整循环。恢复程序会列出缺少的分片编号。

### 手机屏幕出现滚动黑条或频闪

这是相机快门与显示器刷新率不匹配。尝试：

- 把手机录像从 60fps 改为 30fps；
- 把二维码播放速度降为 `--fps 2`；
- 调整屏幕亮度；
- 在相机专业模式中尝试 1/60 或 1/120 秒快门。

### 二维码超出屏幕

把发送端参数降低为：

```powershell
python qr_sender.py data.7z --chunk-size 500 --fps 2
```

分片数量会增加，但二维码密度降低，更容易拍摄和识别。

## 10. 参数建议

| 情况 | 发送端参数 | 手机录像 |
| --- | --- | --- |
| 默认 | `--chunk-size 700 --fps 3` | 1080p/60fps |
| 显示器较小 | `--chunk-size 500 --fps 2` | 1080p/30fps |
| 识别稳定、屏幕清晰 | `--chunk-size 800 --fps 4` | 1080p/60fps |

建议先用一个 10KB 左右的小文件完整测试生成、录像、恢复和哈希校验，再传输正式的 600KB 文件。

## 附录：程序文件

本文档配套两个完整程序：

- `qr_sender.py`：离线电脑播放动态二维码；
- `qr_video_restore.py`：接收电脑从手机录像恢复文件。

代码以配套 `.py` 文件为准，避免从 Markdown 复制时因全角标点或缩进变化导致运行错误。
