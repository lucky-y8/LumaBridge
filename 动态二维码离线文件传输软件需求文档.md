# 动态二维码离线文件传输软件需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | OpticalTransfer（暂定中文名：光码传输） |
| 文档类型 | 软件需求规格说明书（SRS） |
| 目标版本 | v1.0 |
| 开发技术 | Python 3.11、PySide6、qrcode、Pillow、OpenCV、zxing-cpp |
| 优先平台 | Windows 11 x64 |
| 后续平台 | Linux x64 |
| 核心用途 | 在无网络、无 Wi-Fi、USB 不可用的情况下，通过“电脑屏幕动态二维码 + 手机录像”传输文件 |

## 2. 项目背景

当前已经有两个命令行程序：

- `qr_sender.py`：将文件拆分并循环显示为动态二维码；
- `qr_video_restore.py`：从手机录像中识别二维码、去重并恢复文件。

命令行版本存在以下使用问题：

- 普通用户操作门槛较高；
- 二维码可能因屏幕分辨率不足而把状态文字挤出屏幕；
- 视频恢复过程中只显示有效分片数量，没有显示视频扫描进度，容易误认为程序卡死；
- 缺少多视频合并、断点保存和补录后继续恢复能力；
- 发送端和恢复端是两个独立程序；
- 目标电脑需要提前安装 Python 和依赖包。

本项目需要将发送端和恢复端合并为一个 Qt 桌面软件，并分别打包为 Windows 和 Linux 可执行程序。

## 3. 项目目标

1. 提供一个统一的图形界面，同时包含“发送文件”和“恢复文件”功能。
2. 打包后的程序能够在未安装 Python 的电脑上独立运行。
3. 整个传输和恢复过程必须可以完全离线运行，不发起任何网络请求。
4. 通过二维码纠错、分片 CRC32 和整文件 SHA-256，确保恢复结果与原文件逐字节一致。
5. 支持重复二维码自动去重、多段录像合并和恢复状态保存。
6. 视频扫描过程中必须持续显示视频处理进度，避免用户误以为程序卡死。
7. Windows 11 x64 版本优先完成，随后适配 Linux x64。

## 4. 项目范围

### 4.1 v1.0 包含

- 单文件动态二维码发送；
- 单个或多个录像文件恢复同一个原文件；
- 自动去重、分片校验、整文件校验；
- 恢复任务断点保存与继续；
- 中文 Qt 图形界面；
- Windows 11 x64 便携版；
- Linux x64 可执行版本；
- 日志、错误提示和恢复报告。

### 4.2 v1.0 不包含

- 手机端 App；
- 手机摄像头实时直连电脑；
- 网络、蓝牙、USB 或局域网传输；
- 文件加密功能；
- 云端存储、账号登录和在线升级；
- 同时恢复多个不同文件；
- 对视频进行复杂剪辑。

## 5. 目标用户流程

### 5.1 发送流程

1. 用户打开软件并进入“发送文件”。
2. 选择需要传输的文件。
3. 软件计算文件大小、SHA-256、分片数量和预计播放时间。
4. 用户选择稳定、标准或快速模式。
5. 点击“开始播放”。
6. 软件倒计时后全屏循环显示动态二维码。
7. 用户用手机横屏录像，至少录制两个完整循环。
8. 用户退出全屏或停止播放。

### 5.2 恢复流程

1. 用户将手机录像放到接收电脑。
2. 打开软件并进入“恢复文件”。
3. 添加一个或多个录像文件。
4. 点击“开始扫描”。
5. 软件持续显示视频扫描进度和有效分片进度。
6. 所有分片收齐后，软件自动拼接并验证 SHA-256。
7. 验证通过后，用户选择输出位置并保存原文件。
8. 如果分片不完整，用户保存恢复状态，补录后继续扫描。

## 6. 总体界面要求

主窗口使用 PySide6 实现，默认中文界面，至少包含以下区域：

```text
主窗口
├── 发送文件
├── 恢复文件
├── 任务日志
└── 设置/关于
```

界面要求：

- Windows 11 下默认窗口尺寸建议为 1000×700；
- 最低支持 1366×768 分辨率；
- 高 DPI 屏幕下字体和二维码不能模糊；
- 长时间任务必须在后台线程运行，主界面不得无响应；
- 所有关键按钮必须根据任务状态启用或禁用，防止重复启动；
- 错误信息使用用户能够理解的中文说明，不直接抛出 Python 堆栈；
- 详细异常写入日志文件，界面只显示摘要；
- 应用关闭时，如果存在运行任务或未保存状态，必须二次确认。

## 7. 发送端功能需求

### 7.1 文件选择

- 支持通过文件选择器选择任意单个文件；
- 支持把文件拖放到窗口；
- 显示文件名、完整路径、文件大小和最后修改时间；
- 文件名支持中文、空格和常见特殊字符；
- 文件不存在、无权限或读取失败时给出明确提示；
- 不限制文件扩展名；
- 文件超过 10MB 时提示二维码传输耗时较长，但允许用户继续。

### 7.2 文件分析

选择文件后自动计算并显示：

- 文件大小；
- SHA-256；
- 分片大小；
- 总分片数；
- 每秒二维码数量；
- 单循环预计时长；
- 推荐录像时长；
- 当前二维码预计尺寸。

SHA-256 计算必须放入后台线程，大文件计算期间界面保持可操作。

### 7.3 发送模式

预置以下模式：

| 模式 | 分片大小 | 播放速度 | 使用场景 |
| --- | ---: | ---: | --- |
| 稳定 | 500字节 | 2fps | 屏幕较小、反光、识别率较低 |
| 标准 | 700字节 | 3fps | 默认推荐 |
| 快速 | 800字节 | 4fps | 屏幕和手机摄像头质量较好 |

另外提供“自定义”模式：

- 分片大小允许 200～1000 字节；
- 播放速度允许 0.5～8fps；
- 输入超出范围时阻止开始并提示原因。

### 7.4 播放设置

- 支持无限循环播放；
- 支持指定循环次数，建议选项为 1、2、3 次；
- 默认推荐录制两个完整循环；
- 播放前可选 3 秒倒计时；
- 每完成一轮，循环次数加一；
- 支持暂停、继续、上一分片、下一分片和停止；
- 快捷键：`Esc` 退出全屏、空格暂停/继续、左右键切换分片；
- 停止后返回发送界面，不退出整个应用。

### 7.5 全屏二维码界面

全屏界面至少显示：

- 二维码；
- 文件名；
- 当前分片/总分片；
- 当前循环次数；
- 播放或暂停状态；
- SHA-256 的简短显示；
- 快捷键提示。

二维码必须根据当前屏幕可用高度动态计算显示尺寸，不能使用固定 `box_size` 导致状态文本被挤出屏幕。在 1366×768 分辨率下，二维码和全部状态文字必须同时完整可见。

二维码要求：

- 黑色前景、白色背景；
- 保留标准白色静区；
- 使用最近邻方式缩放，禁止插值模糊；
- 二维码必须保持正方形；
- 窗口缩放或切换显示器后重新计算尺寸；
- 全屏播放期间隐藏不必要的窗口装饰。

### 7.6 内存和性能

- 不应一次性生成并长期保存所有高分辨率二维码图片；
- 可以缓存当前帧和少量相邻帧；
- 600KB 文件开始播放等待时间不超过合理范围；
- 播放时界面不卡顿，二维码切换频率应接近用户设置值。

## 8. 二维码数据协议

### 8.1 兼容要求

v1.0 必须兼容现有命令行脚本使用的 `QRF1` 协议。Qt 软件生成的录像应能够被现有 `qr_video_restore.py` 恢复；现有 `qr_sender.py` 生成的录像也应能够被 Qt 软件恢复。

### 8.2 帧格式

每个二维码的文本格式如下：

```text
QRF1|session_id|index|total|file_size|sha256|crc32|filename_b64|chunk_b64
```

字段定义：

| 字段 | 要求 |
| --- | --- |
| `QRF1` | 协议版本标识 |
| `session_id` | 文件 SHA-256 的前16个字符 |
| `index` | 从0开始的分片序号 |
| `total` | 总分片数量 |
| `file_size` | 原文件字节数 |
| `sha256` | 原文件完整 SHA-256 |
| `crc32` | 当前原始分片的8位十六进制 CRC32 |
| `filename_b64` | UTF-8 文件名的 URL-safe Base64，去除尾部等号 |
| `chunk_b64` | 原始分片的 URL-safe Base64，去除尾部等号 |

协议解析必须严格验证字段数量、数值范围、Base64、CRC32、文件标识和元数据一致性。异常二维码不得写入恢复结果。

## 9. 恢复端功能需求

### 9.1 视频管理

- 支持选择常见视频格式，如 MP4、MOV、AVI 和 MKV；
- 支持一次添加多个录像；
- 支持拖放视频到任务列表；
- 支持调整扫描顺序和移除尚未处理的视频；
- 列表显示文件名、时长、分辨率、帧率、状态和扫描进度；
- 同一个视频重复添加时提示用户；
- 视频无法打开或格式不支持时跳过该视频并记录原因，不得导致整个软件崩溃。

### 9.2 扫描控制

- 支持开始、暂停、继续和取消；
- 默认每秒尝试识别12个视频帧；
- 高级设置允许调整为每秒5～30次；
- 扫描任务必须运行在工作线程中；
- 用户暂停后应在短时间内停止继续读取视频；
- 取消任务时保留已识别分片，并询问是否保存恢复状态。

### 9.3 进度显示

必须同时显示两类进度，不能只显示有效分片数量：

1. 视频扫描进度：当前视频已处理时长/总时长、百分比；
2. 分片收集进度：已收集唯一分片/总分片、百分比。

同时显示：

- 当前处理的视频名称；
- 已扫描视频数量/视频总数；
- 已处理帧数/总帧数；
- 已识别二维码次数；
- 新增有效分片数；
- 重复分片数；
- CRC32 错误数；
- 其他文件二维码数量；
- 已运行时间；
- 预计剩余扫描时间；
- 当前状态，例如“正在扫描”“正在处理重复分片”“正在校验文件”。

即使长时间没有获得新分片，视频扫描进度仍必须持续更新，避免在 `789/805（98%）` 等情况下看起来像卡死。

### 9.4 去重和冲突处理

- 使用 `session_id + index` 作为分片唯一标识；
- 同一分片重复出现且数据一致时自动去重；
- 重复分片不得造成恢复文件变大；
- 同一 `session_id`、同一 `index` 出现不同数据时记录为冲突并停止最终恢复；
- 不同 `session_id` 的二维码不得混入当前恢复任务；
- 首次识别到有效二维码后建立当前恢复会话；
- 后续发现其他文件二维码时只记录数量并忽略，必要时提示用户视频可能混入其他任务。

### 9.5 多视频合并

- 同一恢复任务可以依次扫描多个录像；
- 所有录像识别出的有效分片进入同一个去重集合；
- 第二段或后续录像只补充缺失分片，不重复写入已有数据；
- 每段视频扫描完成后显示新增分片数量；
- 所有视频扫描完成但仍缺少分片时，显示缺失编号并允许继续添加补录视频。

### 9.6 恢复状态和断点继续

需要支持保存恢复状态文件，扩展名暂定为：

```text
.qrstate
```

状态文件至少保存：

- 协议版本；
- 文件名、文件大小、SHA-256 和 `session_id`；
- 总分片数；
- 已收集分片及其 CRC32；
- 缺失分片列表；
- 已扫描视频基本信息；
- 创建时间和最后更新时间。

要求：

- 扫描过程中定期自动保存；
- 每个视频处理完成后自动保存；
- 暂停、取消或关闭软件时提示保存；
- 下次打开 `.qrstate` 后可以继续添加录像扫描；
- 状态文件不得使用不安全的 Python Pickle；
- 可采用 SQLite 或经过明确格式定义的 JSON/压缩容器；
- 保存过程应采用临时文件加原子替换，避免软件异常退出造成状态文件损坏。

### 9.7 缺失分片

扫描结束但未收齐时显示：

- 缺失分片数量；
- 缺失分片编号；
- 完成百分比；
- 建议补录方式；
- “保存恢复状态”“添加补录视频”“复制缺失编号”按钮。

缺失编号在界面中以从1开始的方式显示，内部协议仍使用从0开始的索引。

### 9.8 文件恢复和校验

全部分片收齐后：

1. 按索引顺序拼接分片；
2. 按原始文件大小截取最终数据；
3. 校验最终文件大小；
4. 计算并核对 SHA-256；
5. 通过后允许保存文件；
6. 不通过时禁止显示“恢复成功”。

输出要求：

- 默认输出名为原文件名；
- 如果目标文件已存在，不得静默覆盖；
- 提供改名、覆盖和取消选项；
- 写入时先生成临时文件，校验成功后再原子改名；
- 恢复成功界面显示保存路径、文件大小和 SHA-256；
- 支持打开所在目录；
- 如果系统存在 `7z` 命令且文件扩展名为 `.7z`，可以提供可选的压缩包测试按钮；
- 未安装 7z 时不能影响普通文件恢复。

## 10. 日志和错误处理

### 10.1 用户日志

任务日志区域显示：

- 任务开始和结束时间；
- 文件分析结果；
- 二维码播放状态；
- 视频开始和结束扫描；
- 新增、重复、错误和缺失分片统计；
- 状态保存和加载结果；
- 文件恢复和校验结果。

### 10.2 技术日志

- 日志按日期或大小滚动；
- 默认不记录完整分片数据，避免日志泄露文件内容；
- 异常日志包含时间、模块、异常类型和堆栈；
- 提供“打开日志目录”和“导出诊断信息”；
- 诊断信息不得包含原始文件内容和完整二维码载荷。

### 10.3 典型错误

软件需要对以下情况提供明确提示：

- 文件读取失败；
- 视频无法打开；
- 视频中未识别到 `QRF1` 二维码；
- 二维码协议字段错误；
- CRC32 校验失败；
- 分片冲突；
- 视频扫描完成但分片不齐；
- SHA-256 不一致；
- 输出目录无权限；
- 磁盘空间不足；
- 恢复状态文件损坏或版本不兼容；
- 用户主动暂停或取消任务。

## 11. 非功能需求

### 11.1 离线和隐私

- 软件必须完全离线可用；
- 软件不得发起网络请求；
- 不包含遥测、崩溃上传、广告或在线统计；
- 不自动检查更新；
- 文件数据只在本地内存、状态文件和用户选择的输出位置处理。

### 11.2 稳定性

- 单个损坏视频不能导致整个程序崩溃；
- 无效二维码只能被忽略或计数；
- 扫描线程异常必须传递给主界面并安全结束；
- 软件异常退出后，能够从最近一次有效 `.qrstate` 继续；
- 最终 SHA-256 不一致时绝不能报告成功。

### 11.3 性能

- 处理视频时主界面保持响应；
- 进度显示至少每秒更新一次；
- 默认识别参数下合理利用多核 CPU，但避免无上限并发；
- 内存占用不能随着视频时长无限增长；
- 不把整段视频一次性加载进内存；
- 分片集合按唯一索引保存，不保存重复分片数据。

### 11.4 可维护性

- UI、二维码协议、文件分片、视频识别、状态保存和打包配置分模块实现；
- 核心协议和恢复逻辑不得直接写在 Qt 控件类中；
- 核心模块应能脱离 GUI 进行单元测试；
- 关键类型和公共函数需要类型标注；
- 不使用全局可变状态保存任务数据。

## 12. 建议工程结构

```text
optical-transfer/
├── pyproject.toml
├── README.md
├── LICENSES.md
├── src/
│   └── optical_transfer/
│       ├── app.py
│       ├── protocol.py
│       ├── models.py
│       ├── sender/
│       │   ├── chunker.py
│       │   ├── qr_generator.py
│       │   └── player.py
│       ├── receiver/
│       │   ├── video_scanner.py
│       │   ├── collector.py
│       │   ├── state_store.py
│       │   └── restorer.py
│       ├── ui/
│       │   ├── main_window.py
│       │   ├── send_page.py
│       │   ├── recover_page.py
│       │   └── dialogs.py
│       └── resources/
├── tests/
│   ├── test_protocol.py
│   ├── test_chunker.py
│   ├── test_collector.py
│   ├── test_state_store.py
│   └── test_roundtrip.py
├── scripts/
│   ├── build_windows.ps1
│   └── build_linux.sh
└── packaging/
```

该结构是建议，不要求逐字照搬，但必须保证核心逻辑与界面解耦。

## 13. 技术选型要求

| 模块 | 建议技术 |
| --- | --- |
| GUI | PySide6 |
| 二维码生成 | qrcode + Pillow |
| 视频读取 | OpenCV |
| 二维码识别 | zxing-cpp |
| 哈希和校验 | Python 标准库 `hashlib`、`zlib` |
| 后台任务 | QThread、QObject Worker 或等价 Qt 线程方案 |
| 状态文件 | SQLite 或安全、明确版本化的自定义格式 |
| Windows 打包 | PyInstaller，优先目录版，另提供单文件版可选 |
| Linux 打包 | PyInstaller 目录版或 AppImage |
| 自动测试 | pytest |

如果开发中需要替换技术方案，必须说明原因，并保持协议兼容和离线要求。

## 14. 打包和发布要求

### 14.1 Windows 11 x64

- 打包后不依赖系统 Python；
- 提供便携目录版；
- 可选提供单文件 EXE；
- 程序名称、图标和版本信息完整；
- 从包含中文和空格的路径启动时正常；
- 不要求管理员权限；
- 打包后在干净的 Windows 11 x64 虚拟机中测试。

### 14.2 Linux x64

- 必须在 Linux x64 环境构建，不能直接复用 Windows EXE；
- 提供可执行目录包或 AppImage；
- 明确最低支持的发行版或 glibc 版本；
- 测试文件选择、全屏二维码、视频扫描和输出权限。

### 14.3 许可证

- 列出 PySide6、OpenCV、zxing-cpp、qrcode、Pillow 等第三方依赖和许可证；
- 随发布包提供第三方许可证说明；
- 不引入来源不明或许可证不兼容的二进制文件。

## 15. 测试要求

### 15.1 单元测试

至少覆盖：

- URL-safe Base64 编码和解码；
- `QRF1` 帧生成和解析；
- CRC32 正确和错误场景；
- 中文文件名；
- 空文件和最后一个不足分片大小的文件；
- 重复分片去重；
- 相同索引不同内容冲突；
- 不同 `session_id` 过滤；
- 分片排序和文件拼接；
- SHA-256 成功和失败；
- 状态文件保存、重新加载和损坏处理。

### 15.2 集成测试

- 生成二维码图片后直接识别并恢复，完成无视频编解码的往返测试；
- 将二维码帧生成测试视频，再从视频恢复文件；
- 两个循环的视频能够正确去重；
- 两段各缺少不同分片的视频合并后能够恢复；
- 视频中混入其他文件二维码时仍能恢复目标文件；
- 中途保存状态，重启程序后继续恢复；
- 输出文件 SHA-256 与输入文件一致。

### 15.3 人工验收场景

1. 使用约35KB文件，在1366×768屏幕播放，二维码和底部文字全部可见。
2. 使用约600KB的7z文件，按标准模式录制两个循环，能够恢复并通过 SHA-256。
3. 同一二维码重复出现多次，恢复文件大小不得增加。
4. 扫描过程中即使有效分片停在98%，视频扫描百分比仍持续变化。
5. 分片未收齐时，明确显示缺失数量和编号，不生成“恢复成功”的文件。
6. 保存 `.qrstate`，关闭软件后重新打开并继续扫描补录视频。
7. 输出路径已有同名文件时，不得静默覆盖。
8. 断开网络后，发送、扫描、恢复和状态保存全部正常。
9. 在未安装 Python 的干净 Windows 11 x64 系统运行打包版本。
10. Windows 与 Linux 生成的 `QRF1` 数据能够互相恢复。

## 16. 验收标准

满足以下条件才视为 v1.0 完成：

- 发送和恢复集成在同一个 Qt 应用中；
- Windows 11 x64 核心流程可用；
- Linux x64 核心流程可用；
- 与原命令行脚本的 `QRF1` 协议双向兼容；
- 支持多视频、自动去重、冲突检测和断点恢复；
- 视频进度与分片进度同时可见；
- 1366×768 屏幕不会隐藏状态文字；
- 恢复结果必须通过文件大小和 SHA-256 校验；
- 所有自动化测试通过；
- 在无 Python、无网络环境的目标系统完成实际运行测试；
- 提供源代码、构建脚本、使用文档和第三方许可证说明。

## 17. 建议开发阶段

### 第一阶段：核心逻辑

- 抽离并测试 `QRF1` 协议；
- 实现文件分片、二维码生成、分片收集和文件恢复；
- 保证与现有两个脚本兼容。

### 第二阶段：Qt 发送端

- 文件选择和参数计算；
- 二维码全屏播放；
- 动态尺寸适配和控制快捷键。

### 第三阶段：Qt 恢复端

- 视频队列和后台扫描；
- 双进度显示；
- 去重、缺失和冲突统计；
- 多视频补录。

### 第四阶段：断点和稳定性

- `.qrstate` 保存与加载；
- 原子写入；
- 异常、取消和崩溃恢复；
- 日志和诊断信息。

### 第五阶段：测试与打包

- 单元测试和集成测试；
- Windows 11 x64 打包与干净环境验证；
- Linux x64 打包与验证；
- README 和许可证文档。

## 18. 交付物

- 完整源代码；
- `pyproject.toml` 或明确的依赖锁定文件；
- Windows 构建脚本；
- Linux 构建脚本；
- Windows 11 x64 便携版；
- Linux x64 发布包；
- 单元测试和集成测试；
- 用户使用说明；
- 开发和构建说明；
- 第三方许可证清单；
- 已知问题清单。

## 19. 可直接交给 Codex 的执行说明

将本需求文档放入项目根目录，然后向 Codex 提交以下任务：

```text
请完整阅读《动态二维码离线文件传输软件需求文档.md》，并检查项目根目录现有的
qr_sender.py 和 qr_video_restore.py。

先不要直接大规模编码。第一步请完成：
1. 分析现有脚本的 QRF1 协议和可复用逻辑；
2. 输出项目实施计划、模块划分、依赖方案和风险点；
3. 建立项目结构和测试基线；
4. 按需求文档中的五个开发阶段逐步实现；
5. 每个阶段完成后运行测试并汇报结果；
6. 不得修改 QRF1 字段定义，除非同时保留向后兼容；
7. 所有视频识别任务必须在后台线程运行，禁止阻塞 Qt 主线程；
8. 最终必须提供 Windows 11 x64 和 Linux x64 的构建脚本。

开发过程中如需求存在冲突或必须做重大技术取舍，先说明影响和备选方案，再继续实施。
```
